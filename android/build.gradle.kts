// Top-level build file — no toolchain (Android Studio / Gradle) is available
// in the environment that wrote this app, so it hasn't been built here; open
// this folder in Android Studio (see ../README.md) to sync and run it.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
